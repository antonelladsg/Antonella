let contador = 0
while (contador <=3) {
   console.log ("hola")
   contador = contador + 1
    
}

const usuario ="";
const contraseña = "";
let intentos = 0;
const maxIntentos = 3;
let accesoContenido = false;
do{
const pedirUsuario = prompt ("Ingrese su usuario");
const pedirContraseña = prompt ("Ingrese su contraseña");
} while (a);

if (contraseña == pedirContraseña && usuario == pedirUsuario) {
    let accesoContenido = true;
    alert = "Bienvenido"

} else {
    intentos++; 
    if (intentos < maxIntentos)
       alert ("Incorrecto, intentos restantes: " + (maxIntentos - intentos) )

    } while (intentos < maxIntentos && !accesoContenido);



